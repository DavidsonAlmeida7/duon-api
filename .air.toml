root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -buildvcs=false -o ./tmp/app ./cmd/api"
  bin = "tmp/app"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "migrations"]

[log]
time = true

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"
